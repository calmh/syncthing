name: PR metadata

on:
  pull_request:
    types:
      - opened
      - edited
      - reopened

permissions:
  contents: read
  pull-requests: write

jobs:

  #
  # Set labels on PRs, which are used to categorise release notes
  #

  labels:
    name: Set labels
    runs-on: ubuntu-latest
    steps:
    - uses: srvaroa/labeler@v1
      env:
        GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
